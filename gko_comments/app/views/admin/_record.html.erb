<%
   title = record.title || "!(#{record.read_attribute(:title, default_locale)})"
%>
<tr>
  <td><%= glink_to(title, gurl_for([:edit, :admin, site, record.commentable, record])) %></td>
  <td><%= icons_to_actions([:destroy, :edit], record) %></td>
</tr>